version: 2
models:
  - name: stg_orders
    description: one unique order per row
    columns:
      - name: orderid
        description: the primary key for stg_oders
      - name: shipmode
        description: desc